# For containerized development (use this with "just start")
DATABASE_URL="postgres://admin:pansamochodzik123@db:5432/local"

# For local development (use this with "just local-dev" or "npm run dev")
# DATABASE_URL="postgres://admin:pansamochodzik123@localhost:5432/local"

# Uncomment to enable additional debug logging
# DEBUG=true

# Docker platform setting (for multi-architecture support)
# Leave unset to use host's native architecture (recommended)
# Set to 'linux/amd64' or 'linux/arm64' to force a specific architecture
# DOCKER_DEFAULT_PLATFORM=linux/amd64
